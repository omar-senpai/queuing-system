apiVersion: v1
kind: Service
metadata:
  name: {{ include "queuing-system.fullname" . }}
  labels:
    {{- include "queuing-system.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "queuing-system.selectorLabels" . | nindent 4 }}
